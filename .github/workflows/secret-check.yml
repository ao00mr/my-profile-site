name: Secret Check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Secret を安全に参照
        env:
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
        run: |
          echo "Secret is configured."
          echo "Masked value: $MY_API_KEY"
